{
  "name": "arvind",
  "buildCommand": "npx graphql-codegen && npx prisma generate && npx prisma migrate deploy && npm run build",
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts"
}
